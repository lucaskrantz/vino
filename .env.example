# Used only by `vino classify`. Never commit the real value.
OPENAI_API_KEY=

# Used by `vino classify --provider openrouter`.
OPENROUTER_API_KEY=

# Pin this explicitly for reproducible reruns; it can also be passed with --model.
VINO_VISION_MODEL=gpt-4o-mini-2024-07-18
